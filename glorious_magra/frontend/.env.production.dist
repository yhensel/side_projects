# Copy to .env.production.local before running `npm run build` for OVH deployment.
# Origin only (no trailing path) — request paths already start with /api/...
# API hostname points at the Symfony public/ folder.
VITE_API_BASE_URL=https://api.glorious-magra.yhensel.com
